//C program to find the remainder when number is divided by 2
#include<stdio.h>
int main()
{
    int num;
    printf("Enter the number\n");
    scanf("%d", &num);
    printf("Remainder when number is divded by 2 is: %d\n", num%2);
    return 0;
}
